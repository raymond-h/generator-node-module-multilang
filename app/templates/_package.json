{
	"name": "<%= name %>",
	"version": "0.0.0",
	"devDependencies": {
		"grunt": "*",
		"load-grunt-tasks": "*",
		"grunt-contrib-coffee": "*",
		"grunt-coffeelint": "*",
		"grunt-contrib-watch": "*",
		"chai": "*",
		"grunt-mocha-test": "*"
	},
	"dependencies": {
<% _.forEach(deps, function(dep, i) { print('\t\t"' + dep + '": "*"' + (i < deps.length-1 ? ',\n' : '')); }); %>
	}
}
